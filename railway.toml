[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Migrations run inside the container on api-server startup (index.ts) before
# accepting traffic. A failed migration blocks the deploy so code never ships
# against a stale schema.
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
