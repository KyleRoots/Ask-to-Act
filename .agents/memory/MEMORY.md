- [Bullhorn OAuth](bullhorn-oauth.md) — headless auth end-to-end: authorization_code + refresh tokens; first-time consent approved by POSTing the consentForm server-side (browser "Agree" bounces); incl. lockout self-unlock.
- [Bullhorn résumés & Files API](bullhorn-resumes-files.md) — résumés live in candidate.description AND as file attachments; list key is `EntityFiles` (miss it → count 0); REST limit 120/60s.
- [Candidate search & résumé PII](bullhorn-candidate-search.md) — clearance/skills live in résumé TEXT (description)+skillSet/comments, no structured field; field-scope every Lucene term; ChatGPT withholds bulk-PII → use get_candidate_resume VERIFY (highlight) excerpt mode.
- [ChatGPT connector hosting](chatgpt-connector-hosting.md) — repeated Connect/Allow popups & unfinished runs = dev endpoint not always-on + per-call approval; fix = deploy a stable URL + click "Always allow" (transport is stateless → Autoscale safe).
- [MCP tool annotations](mcp-tool-annotations.md) — read-only MCP tools MUST declare readOnly/destructive/openWorld hints or ChatGPT tags them DESTRUCTIVE → OpenAI safety-blocks reads + nags approval; inject via a wrapper, verify with tools/list.
- [Bullhorn custom-field labels](bullhorn-custom-fields.md) — custom API names need meta `label`; "Internal Department" differs per entity (JobOrder/Placement=correlatedCustomText1, others=customText1, Candidate=customText3); describe_entity exposes configuredCustomFields.
- [Write-phase requirements](bullhorn-write-phase-requirements.md) — future writes MUST enforce each user's own Bullhorn permission gates (differentiator); per-tenant onboarding must front-load full field-mapping via configuredCustomFields/meta labels.
- [Tool-output size ceiling](response-size-limits.md) — ONE cause of ChatGPT "blocked by safety layer" is its client-side SIZE cap dropping big responses; fix = compact JSON + scoped fields + pagination (not unlocking access).
- [Search query gotchas](bullhorn-search-query-syntax.md) — Bullhorn /search returns 0 for ALL-negative queries; fix = FLAT anchor `id:[1 TO *] AND NOT…` (parenthesized all-negative ALSO returns 0); /query path is fine; + disambiguates the 3 ChatGPT "blocked" messages.
- [Bullhorn report library](bullhorn-report-library.md) — v1 baked-in report tools + locked metric defs (open jobs / confirmed placements / active opps) + fetch-vs-count techniques (placements fetch+aggregate, submissions count-only).
- [api-server MCP+REST doors](api-server-rest-mcp.md) — one set of read-only Bullhorn fns behind BOTH MCP and an additive /api/v1 REST/OpenAPI door; openapi.yaml=source of truth via orval; keep doors in lockstep; orval gotcha: date params use string pattern, not format:date.
- [Metric definition lock + speed](bullhorn-metric-definition-drift.md) — locked headline numbers (open jobs 398, active opps 23, placements 98 YTD/2650, soft-deleted excluded) enforced server-side on EVERY query shape, not tool-description conventions; count_entity speed = bounded-concurrency (limit 5) group fan-out + Promise.all annotations; never approximate a count to save a round-trip.
- [Record-fetch cap](bullhorn-fetch-cap.md) — 50/call is SILENTLY TRIMMED (never rejected) via zod `.transform(capFetch)` + `_truncatedNote` in runTool; clamp lives ONLY at the MCP schema layer so internal group-discovery (count=500) & report paging stay un-clamped (else breakdowns stop summing).
