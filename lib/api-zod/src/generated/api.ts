/**
 * Generated by orval v8.9.1 🍺
 * Do not edit manually.
 * Api
 * API specification
 * OpenAPI spec version: 0.1.0
 */
import * as zod from 'zod';


/**
 * Returns server health status
 * @summary Health check
 */
export const HealthCheckResponse = zod.object({
  "status": zod.string()
})


/**
 * Returns the catalog of pre-built reports and their parameters.
 * @summary List available reports
 */
export const ListReportsResponse = zod.record(zod.string(), zod.unknown()).describe('Report or count payload. The exact shape varies by endpoint.')


/**
 * Year-to-date staffing scorecard by department: confirmed placements (Contract / Contract-to-Hire / Direct Hire), open jobs, active opportunities, and a demand-vs-delivery ratio.
 * @summary Staffing scorecard
 */
export const getStaffingScorecardQueryYearMin = 2000;
export const getStaffingScorecardQueryYearMax = 2100;



export const GetStaffingScorecardQueryParams = zod.object({
  "year": zod.coerce.number().min(getStaffingScorecardQueryYearMin).max(getStaffingScorecardQueryYearMax).optional().describe('Calendar year; defaults to the current year.')
})

export const GetStaffingScorecardResponse = zod.record(zod.string(), zod.unknown()).describe('Report or count payload. The exact shape varies by endpoint.')


/**
 * Confirmed placements over a period, by department and employment type.
 * @summary Placements report
 */
export const getPlacementsReportQueryStartDateRegExp = new RegExp('^\\d{4}-\\d{2}-\\d{2}$');
export const getPlacementsReportQueryEndDateRegExp = new RegExp('^\\d{4}-\\d{2}-\\d{2}$');


export const GetPlacementsReportQueryParams = zod.object({
  "startDate": zod.coerce.string().regex(getPlacementsReportQueryStartDateRegExp).optional().describe('Inclusive start date (YYYY-MM-DD). Defaults to the start of the current year.'),
  "endDate": zod.coerce.string().regex(getPlacementsReportQueryEndDateRegExp).optional().describe('Inclusive end date (YYYY-MM-DD). Defaults to today.'),
  "status": zod.enum(['confirmed', 'all']).optional().describe('\'confirmed\' (default) or \'all\'.')
})

export const GetPlacementsReportResponse = zod.record(zod.string(), zod.unknown()).describe('Report or count payload. The exact shape varies by endpoint.')


/**
 * Current open requisitions by department and by employment type.
 * @summary Open jobs / demand report
 */
export const GetOpenJobsReportResponse = zod.record(zod.string(), zod.unknown()).describe('Report or count payload. The exact shape varies by endpoint.')


/**
 * Active sales opportunities by department and by stage.
 * @summary Sales pipeline report
 */
export const GetSalesPipelineReportResponse = zod.record(zod.string(), zod.unknown()).describe('Report or count payload. The exact shape varies by endpoint.')


/**
 * Open requisitions bucketed by age, with stale (>90 days) reqs by department.
 * @summary Job aging report
 */
export const GetJobAgingReportResponse = zod.record(zod.string(), zod.unknown()).describe('Report or count payload. The exact shape varies by endpoint.')


/**
 * Recruiters ranked by confirmed placements over a period, with submission activity.
 * @summary Recruiter leaderboard
 */
export const GetRecruiterLeaderboardQueryParams = zod.object({
  "startDate": zod.date().optional().describe('Inclusive start date (YYYY-MM-DD). Defaults to the start of the current year.'),
  "endDate": zod.date().optional().describe('Inclusive end date (YYYY-MM-DD). Defaults to today.')
})

export const GetRecruiterLeaderboardResponse = zod.record(zod.string(), zod.unknown()).describe('Report or count payload. The exact shape varies by endpoint.')


/**
 * Exact count for a Lucene query, optionally broken down by a field. Read-only; returns totals without the underlying records.
 * @summary Count Bullhorn records
 */
export const CountEntitiesBody = zod.object({
  "entityType": zod.string().describe('Searchable entity: Candidate, ClientContact, ClientCorporation, JobOrder, JobSubmission, Placement, Lead, Opportunity, or Note.'),
  "query": zod.string().optional().describe('Lucene query string. Omit to count all records of the entity.'),
  "groupBy": zod.string().optional().describe('Field to break the count down by (e.g. status, correlatedCustomText1).'),
  "groupValues": zod.array(zod.string()).optional().describe('Known values for groupBy, for an exact (non-sampled) breakdown.')
})

export const CountEntitiesResponse = zod.record(zod.string(), zod.unknown()).describe('Report or count payload. The exact shape varies by endpoint.')


